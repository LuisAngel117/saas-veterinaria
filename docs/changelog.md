# Changelog

## Unreleased
- T1: bootstrap de estructura docs/scripts + project-lock + verificadores (placeholders iniciales).
- T2: docs base reales (brief + BRD con BRD-REQ + arquitectura + seguridad + dominio + UX + runbook + permisos + entrega) + RTM inicial + state snapshot + masters BACK/FRONT en DRAFT.

<!-- EOF -->
